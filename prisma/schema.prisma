generator client {
  provider   = "prisma-client-js"
  engineType = "library"
}

datasource db {
  provider = "mysql"
}

model Certificate {
  id          String   @id
  frontImgUrl String
  backImgUrl  String
  createdAt   DateTime @default(now())
}
