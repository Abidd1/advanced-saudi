generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Certificate {
  id          String   @id
  frontImgUrl String
  backImgUrl  String
  createdAt   DateTime @default(now())
}
