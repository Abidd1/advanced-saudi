generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Certificate {
  id          String   @id
  frontImgUrl String
  backImgUrl  String
  createdAt   DateTime @default(now())
}
