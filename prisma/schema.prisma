generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Certificate {
  id          String   @id
  frontImgUrl String
  backImgUrl  String?
  createdAt   DateTime @default(now())
}
